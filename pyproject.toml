[project]
name = "duplicate-finder"
version = "0.1.0"
description = "Command-line tool to find duplicate files by content"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
duplicate-finder = "duplicate_finder:main"
